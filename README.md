# Project Structure

The project is now split into:

- `frontend/`: Vite UI app.
- `backend/`: Node.js API server for OpenAI calls.

## Run

1. Start the backend from `backend/` with `npm install` then `npm run dev`.
2. Start the frontend from `frontend/` with `npm install` then `npm run dev`.
